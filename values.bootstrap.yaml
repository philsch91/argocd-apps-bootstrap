---
automated: true
metadata:
  name: karpenter-bootstrap
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
  project: default
  sources:
    - helm:
        valueFiles:
          - $values/values.apps.applications.yaml
      path: .
      repoURL: https://hostname.tld/argocd-apps-bootstrap.git
      targetRevision: main
    - ref: values
      repoURL: https://hostname.tld/argocd-apps-bootstrap.git
      targetRevision: main
  syncPolicy:
    automated: true
    selfHeal: true
    prune: true
    allowEmpty: false
